from base import BaseHandler

class HomePageHandler(BaseHandler):
    def get(self):
        if self.logged_in:
#            self.render('homepage.html')
            self.render('main_div.html')
        else:
            self.redirect('/')

